# 智扫通机器人智能客服 · 开发文档

> 本文档讲"**整体怎么串起来**"：流程、时序、参数如何流动、模块如何调用。
> 使用建议：先读第 3、4、5、6 章的**四条链路**，再看细节表格。

---

## 1. 项目概述

一个"扫地机器人智能客服"：用户在前端网页提问，Agent（ReAct 智能体）自主决定调用哪些工具（知识库检索 / 天气 / 用户信息 / 使用记录），结合资料生成回答；当用户要"使用报告"时，自动切换成"报告写手"模式输出 Markdown 报告。

**技术栈：**

| 组件 | 选型 | 作用 |
|---|---|---|
| Web 前端 | Streamlit | Python 直接写网页，支持流式输出 |
| Agent 框架 | LangChain 1.x（底层 LangGraph） | create_agent 组装、ReAct 循环、中间件 |
| 对话模型 | DeepSeek（deepseek-v4-flash） | 思考、决策、生成回答 |
| 向量模型 | 阿里 DashScope（text-embedding-v4） | 文字 → 1024 维向量 |
| 向量库 | Chroma（本地持久化） | 存知识库向量、相似度检索 |
| 文档解析 | PyPDFLoader / TextLoader | PDF / TXT → Document |
| 天气数据 | Open-Meteo（免费，无需 API Key） | 实时天气：气温 / 湿度 / 风向风速 |
| 外部数据 | SQLite | 用户档案与使用记录（由 CSV 导入） |
| 环境管理 | uv | 虚拟环境 + 依赖锁 |

---

## 2. 目录结构与文件职责

```
智扫通机器人/
├── app.py                          # Streamlit 入口：页面 + 聊天交互 + 流式显示
├── config/                         # —— 所有"会变的参数" ——
│   ├── chroma.yml                  #    向量库 / 分块 / 去重 参数
│   ├── rag.yml                     #    模型名称
│   ├── prompts.yml                 #    四份提示词文件路径
│   └── agent.yml                   #    外部数据路径 + 演示用户 + 裁剪上限 + 思考开关
├── data/                           # 知识库原始资料（6 个 txt/pdf）
│   └── external/
│       ├── records.csv             #    用户使用记录源数据（导入 SQLite 的源）
│       └── demo.db                 #    SQLite 演示库（派生，不进版本库）
├── model/
│   └── factory.py                  # 模型工厂：产出 chat_model / embed_model
├── rag/
│   ├── vector_store.py             # 向量库服务：入库（读文件→切块→向量化→存）+ 取检索器
│   └── rag_service.py              # RAG 总结服务：检索 → 拼上下文 → 模型总结
├── agent/
│   ├── react_agent.py              # 智能体本体：create_agent 组装 + 流式执行入口
│   └── tools/
│       ├── agent_tools.py          # 7 个工具（薄壳：只做参数转发与结果整形）
│       ├── external_data.py        # 外部数据层：SQLite 建表 / CSV 导入 / 用户与记录查询
│       └── middleware.py           # 4 个中间件（门卫）：日志监控 + 上下文裁剪 + 信号弹 + 动态提示词
├── utils/                          # —— 通用工具层（不含业务） ——
│   ├── path_tool.py                # 项目根定位 + 相对路径→绝对路径
│   ├── config_handler.py           # 读 4 份 yml → 4 个全局配置对象
│   ├── logger_handler.py           # 全局日志器（控制台 INFO / 文件 DEBUG）
│   ├── file_handler.py             # 文件发现 / MD5 指纹 / PDF/TXT 加载器
│   └── prompt_loader.py            # 读 4 份提示词 txt → 字符串
├── prompts/                        # —— 提示词文案 ——
│   ├── main_prompt.txt             # Agent 主系统提示词（客服+ReAct 规则+工具手册）
│   ├── rag_summarize.txt           # RAG 总结提示词（含 {input} {context} 占位符）
│   ├── report_prompt.txt           # 报告写手提示词（换装后的制服）
│   └── thinking_rule.txt           # 思考过程输出规则（由 show_thinking 开关控制是否拼上）
├── .env                            # 密钥（不进代码/配置/版本库）
├── logs/                           # 运行日志（每天一个文件）
├── chroma_db/                      # 向量库落盘位置
└── md5.txt                         # 已入库文件的指纹账本
```

**依赖方向（单向）：** `app.py → agent → rag → model → utils`，下层永远不知道上层存在。

---

## 3. 链路零：进程启动时序（import 触发的初始化）

启动命令：`uv run streamlit run app.py`。在**任何页面渲染之前**，一连串模块级初始化已被 import 链触发：

```
app.py 开始执行
  │  import agent.react_agent
  │     import agent.tools.agent_tools
  │        import rag.rag_service
  │           import rag.vector_store
  │              import model.factory
  │                 load_dotenv(项目根/.env)      → 密钥进环境变量
  │                 chat_model / embed_model 创建
  │              import utils.config_handler
  │                 get_abs_path → 读 4 份 yml   → rag_conf/chroma_conf/prompts_conf/agent_conf
  │              import utils.logger_handler      → logs/ 建好，全局 logger 上岗
  │              VectorStoreService()             → 连接 chroma_db（不存在则创建）
  │              load_rag_prompt()                → 读 rag_summarize.txt
  │              chain = prompt | model | parser  → RAG 链焊好
  │        【模块级单例】rag = RagSummarizeService()   ← 以上全部发生在这一刻
  │        import agent.tools.external_data      → SQLite 数据层就绪（模块级不建连接，查询时才连）
  │        定义 7 个 @tool 工具对象（含 Open-Meteo 常量与 WMO 天气码表）
  │     import agent.tools.middleware            → 定义 4 个钩子函数
  │  （ReactAgent 类等待被实例化）
  │
  st.title(...) → 页面开始渲染
  if "agent" not in st.session_state:
      st.session_state["agent"] = ReactAgent()   ← create_agent 真正组装智能体
```

**要点：** 所有"全局单例"（chat_model、embed_model、logger、rag、4 个配置对象）都是"模块被 import 时执行一次"的产物——全项目共享同一份。

---

## 4. 链路一：知识库入库（离线流程，跑一次）

**入口：** `vector_store.py` 的 `if __name__ == '__main__'`（或直接运行该文件）

```
① 找文件      listdir_with_allowed_type(data目录, ("txt","pdf"))
               → 参数来自 chroma.yml: data_path / allow_knowledge_file_type
② 算指纹      get_file_md5_hex(文件) → 32位十六进制字符串
③ 查账本      check_md5_hex(指纹) 读 md5.txt（chroma.yml: md5_hex_store）
               命中 → 记日志"已存在，跳过" → continue（省钱机制）
④ 读文档      txt_loader / pdf_loader → list[Document]（page_content + metadata）
⑤ 切块        spliter.split_documents(文档)
               → 参数来自 chroma.yml: chunk_size=200 / chunk_overlap=40 / separators
               → 优先在 \n\n、\n、. 等语义边界切开；超长时按下一级分隔符"递归"再切
⑥ 向量化+入库  vector_store.add_documents(块列表)     ← 调用 embed_model 批量转向量
               写入 chroma_db/（persist_directory，经 get_abs_path 锚定项目根）
⑦ 记账        save_md5_hex(指纹) 追加一行到 md5.txt  ← 先入库、后记账
```

**产出：** `chroma_db/`（约 343 个向量块）+ `md5.txt`（6 行指纹）。
**验收：** 第二次运行应全部"已存在，跳过加载"。

**检索侧（备用）：** `get_retriever()` = `Chroma.as_retriever(search_kwargs={"k": chroma.yml 的 k=4})`。

---

## 5. 链路二：用户问答主流程（在线核心，最详细）

### 5.1 全景图

```
[用户输入] → app.py → ReactAgent.execute_stream → agent.stream（LangGraph）
                                                        │
                        ┌───────────────────────────────┘
                        ▼   ReAct 循环（每轮 = 一次模型调用 + 可能一次工具调用）
        ┌─ @before_model（trim_history）：消息超上限则裁剪 ───────┐
        │        ▼                                              │
        │  @dynamic_prompt 发提示词（客服 or 报告写手）           │
        │        ▼                                              │
        │  模型思考（逐 token 流式输出）                          │
        │        ▼                                              │
        │  要调工具？──否──→ 生成最终回答（流式输出）→ 循环结束    │
        │        │是                                            │
        │        ▼                                              │
        │  @wrap_tool_call（monitor_tool）：日志 + 信号弹检测     │
        │        ▼  执行工具（7 选 1）                            │
        │  工具返回结果（ToolMessage）                           │
        │        ▼                                              │
        │  @before_model（log_before_model）：记录"带 N 条消息"    │
        └────────┘（观察结果，回到循环顶）
                        │
                        ▼
        execute_stream 逐段 yield → capture 中转 → write_stream → 网页气泡（打字机）
```

### 5.2 app.py 分步（Streamlit 执行模型：每次交互重跑整个脚本）

| 步骤 | 代码动作 | 关键点 |
|---|---|---|
| 1 | `st.title / st.divider` | 每行 = 页面一个元素 |
| 2 | `if "agent" not in st.session_state` | 惰性初始化 Agent；重跑时复用（Storage 不能丢） |
| 3 | `if "messages" not in st.session_state` | 聊天历史列表（**键名 messages，注意复数**） |
| 4 | 循环重绘历史 | 页面是白板：每次重跑都要把历史重新画出来 |
| 5 | `prompt = st.chat_input(...)` | 平时返回 None；用户提交后重跑，本行拿到输入文字 |
| 6 | `if prompt:` → 画用户气泡 + 存历史 | 先上屏、再留档 |
| 7 | `with st.spinner(...)` | 转圈提示（覆盖工具调用的"静默期"） |
| 8 | `res_stream = agent.execute_stream(st.session_state["messages"])` | 传**整段会话**（末尾已是本次提问）；**只是接好管子，还没执行**（生成器惰性） |
| 9 | `write_stream(capture(res_stream, response_messages))` | 点火：开始"要货"，逐字显示 |
| 10 | `"".join(response_messages)` | 流完后拼出完整回复 |
| 11 | 存 assistant 消息 + `st.rerun()` | 重跑后历史循环把两条消息标准重绘 |

### 5.3 流式"要货链"（惰性拉取）

```
write_stream ──要 1 段──▶ capture（中转站）
                            ├─ 缓存整段到 response_messages（存档用）
                            └──要 1 段──▶ execute_stream 的 yield
                                             └──要 1 chunk──▶ agent.stream（LangGraph）
                                                               └──▶ 模型逐 token 生成
```

每层都是同一形状：`for x in 上游：… yield 给下游`。**最下游开始要货，整条链才逐级苏醒。**

打字机效果来自模型逐 token 生成本身，capture 只做缓存与透传，**不额外加延时**（早期实现里逐字符 `time.sleep` 是多余的，已移除）。

### 5.4 Agent 内部（每轮循环发生的事）

1. **模型调用前**：`@before_model`（trim_history）检查消息数，超上限则裁掉最早的部分
   - 删除必须用 `RemoveMessage(id=...)`——`messages` 是 `add_messages` reducer（追加语义），直接返回裁剪后的列表只会**越删越多**
   - 裁剪边界若落在 `ToolMessage` 上，要把它一并裁掉：它的 `AIMessage` 父消息已被裁走，孤儿工具消息会让模型 API 返回 400
2. `@dynamic_prompt`（report_prom_switch）→ **组装本轮提示词**：先读 `runtime.context["report"]` 决定业务提示词（客服 / 报告写手），再按 `agent.yml` 的 `show_thinking` 决定是否拼上思考过程规则。两个维度互相正交
3. 模型调用前：`@before_model`（log_before_model）记日志（消息条数 + 最新消息类型）
4. 模型思考：输出思考文字（main_prompt 输出规则要求的）+（可能）tool_calls 指令
5. 若调工具：`@wrap_tool_call`（monitor_tool）拦截 → 记日志 → `handler(request)` 放行真正执行 → 返回 ToolMessage 进消息流水
6. 循环回到 1（最多约 5 轮，main_prompt 规定超限回复"我不知道"）

### 5.5 消息流水（state["messages"]）三兄弟

| 类型 | 谁 | 内容示例 |
|---|---|---|
| HumanMessage | 用户 | "清扫后地面还有灰尘、碎屑" |
| AIMessage | 模型 | 思考文字 / 最终回答 /（内含 tool_calls 指令时 content 为空） |
| ToolMessage | 工具 | 检索资料原文 / 天气 / 使用记录 |

**注意：** `messages` 字段用的是 `add_messages` reducer（**追加语义**）。所以删除消息**不能**用"返回裁剪后的列表"——那只会往后追加，反而越删越多；必须用 `RemoveMessage(id=...)`，它是 reducer 认识的特殊指令。详见 5.4 第 1 步。

---

## 6. 链路三：报告模式（信号弹机制，三件套协作）

触发语示例："生成我的使用报告"。`execute_stream` 启动时递入记事板：`context={"report": False}`。

```
用户："生成我的使用报告"
  │
  ① @dynamic_prompt 读记事板=False → 发【客服提示词 main_prompt】
  │    其中"报告生成强约束"规定流程：ID → 月份 → fill_context_for_report → fetch_external_data
  │
  ② 模型按流程依次调用：
  │    get_user_id        → monitor_tool 放行（此时不触发）
  │    get_current_month  → 放行
  │    fill_context_for_report  ← ★ 信号弹！
  │        monitor_tool 检查 request.tool_call["name"] == "fill_context_for_report"
  │        → request.runtime.context["report"] = True    【翻记事板】
  │
  ③ fetch_external_data → 拿到该用户该月使用记录（ToolMessage）
  │
  ④ 下一轮循环 @dynamic_prompt 读记事板=True
  │    → 返回 load_report_prompts()（report_prompt.txt） 【换装！】
  │
  ⑤ 模型从此"穿着报告写手制服"继续 → 输出 Markdown 报告 → 流式显示
```

**三段式记忆：** 开局递记事板（react_agent）→ 翻页（monitor_tool）→ 读页（report_prom_switch）。

---

## 7. 参数总表（谁读取、干嘛用）

### 7.1 `config/rag.yml`

| 参数 | 值 | 读取者 | 用途 |
|---|---|---|---|
| chat_model_name | deepseek-v4-flash | model/factory.py | ChatOpenAI 的 model 参数 |
| embedding_model_name | text-embedding-v4 | model/factory.py | DashScopeEmbeddings 的 model 参数 |

### 7.2 `config/chroma.yml`

| 参数 | 值 | 读取者 | 用途 |
|---|---|---|---|
| collection_name | agent | vector_store.py | Chroma 集合名（"表名"） |
| persist_directory | chroma_db | vector_store.py（**经 get_abs_path**） | 向量库落盘目录 |
| k | 4 | vector_store.py → retriever | 每次检索返回条数 |
| data_path | data | vector_store.py | 知识文件所在目录 |
| md5_hex_store | md5.txt | vector_store.py | 指纹账本文件 |
| allow_knowledge_file_type | ["txt","pdf"] | vector_store.py | 认哪些后缀 |
| chunk_size | 200 | vector_store.py → splitter | 每块最大字符数 |
| chunk_overlap | 40 | vector_store.py → splitter | 相邻块重叠字符数 |
| separators | ["\n\n","\n",".","?","!"," "] | vector_store.py → splitter | 切分优先级（递归） |

### 7.3 `config/prompts.yml`

| 参数 | 值 | 读取者 | 用途 |
|---|---|---|---|
| main_prompt_path | prompts/main_prompt.txt | prompt_loader.load_system_prompts | Agent 初始提示词 |
| rag_summarize_prompt_path | prompts/rag_summarize.txt | prompt_loader.load_rag_prompt | RAG 总结链的模板 |
| report_prompt_path | prompts/report_prompt.txt | prompt_loader.load_report_prompts | 报告模式换装用 |
| thinking_rule_path | prompts/thinking_rule.txt | prompt_loader.load_thinking_rule | 思考过程输出规则（`show_thinking` 开启时才拼上） |

### 7.4 `config/agent.yml` 与 `.env`

| 参数 | 读取者 | 用途 |
|---|---|---|
| agent.yml: external_data_path = data/external/records.csv | external_data.load_from_csv | 使用记录的**源数据**（CSV） |
| agent.yml: external_db_path = data/external/demo.db | external_data._db_path | SQLite 库位置（派生数据，不进版本库） |
| agent.yml: demo_user_id = "1001" | external_data.get_current_user_id | 当前用户 ID（无用户系统，固定为演示账号） |
| agent.yml: max_history_messages = 20 | middleware.trim_history | 单次发给模型的**消息数上限**，超出则裁剪最早的对话 |
| agent.yml: show_thinking = true | middleware.report_prom_switch | 是否拼上「思考过程必须可见」规则。默认开启（演示要看得见推理）；作为客服产品上线建议关闭 |
| .env: DEEPSEEK_API_KEY | factory（load_dotenv + os.getenv） | 调 DeepSeek 的钥匙 |
| .env: DASHSCOPE_API_KEY | factory（同上） | 调 DashScope 向量化的钥匙 |

**路径纪律：** 配置里一律写**相对路径**，代码里一律过 `get_abs_path()` → 绝对路径锚定项目根（不受启动目录影响）。

---

## 8. 模块调用关系（谁用谁）

```
app.py
 └── ReactAgent（react_agent.py）
      ├── create_agent(model=chat_model,
      │               system_prompt=load_system_prompts(),
      │               tools=[7 个工具],
      │               middleware=[trim_history, log_before_model, monitor_tool, report_prom_switch])
      ├── tools → rag_summarize → RagSummarizeService（rag_service.py）
      │                              ├── VectorStoreService.get_retriever（vector_store.py）
      │                              └── chain = PromptTemplate | chat_model | StrOutputParser
      ├── tools → get_weather → Open-Meteo API（HTTP，无需 Key）
      ├── tools → fetch_external_data / get_user_* → external_data（external_data.py）→ SQLite
      └── middleware → prompt_loader（load_system_prompts / load_report_prompts / load_thinking_rule）

utils 被所有层引用：path_tool（路径）→ config_handler（配置）→ logger_handler（日志）
model/factory.py 被 rag 与 agent 引用（chat_model / embed_model）
```

---

## 9. 核心方法速查

### utils 层

| 方法 | 输入 → 输出 | 职责 |
|---|---|---|
| `get_project_root()` | 无 → 绝对路径 | 由 `__file__` 反推项目根 |
| `get_abs_path(相对路径)` | str → str | 锚定项目根的绝对路径 |
| `get_logger(...)` / 全局 `logger` | — | 控制台 INFO + 文件 DEBUG 双出口 |
| `load_*_config()` ×4 / `*_conf` ×4 | yml → dict | 配置读取 |
| `load_system_prompts / load_rag_prompt / load_report_prompts / load_thinking_rule` | 读 txt → str | 提示词读取（4 份） |
| `get_file_md5_hex(路径)` | 文件 → 指纹 | 内容指纹（分块读取） |
| `listdir_with_allowed_type(目录, 后缀元组)` | → 路径元组 | 文件发现 |
| `txt_loader / pdf_loader(路径)` | → list[Document] | 文档加载 |

### model 层

| 方法 | 输出 | 说明 |
|---|---|---|
| `ChatModelFactory().generator()` | chat_model | 连 DeepSeek（OpenAI 协议 + base_url） |
| `EmbeddingsFactory().generator()` | embed_model | DashScope 向量化 |

### rag 层

| 方法 | 输入 → 输出 | 说明 |
|---|---|---|
| `VectorStoreService.load_documents()` | 无 → 无 | 入库全流程（见链路一） |
| `VectorStoreService.get_retriever()` | → retriever | `as_retriever(k=4)` |
| `RagSummarizeService.rag_summarize(问题)` | str → str | 检索 → 拼 context → chain.invoke → 总结 |
| `RagSummarizeService.retriever_docs(问题)` | str → list[Document] | 检索 + 调试打印 |

### external_data 层（外部数据）

| 方法 | 输入 → 输出 | 职责 |
|---|---|---|
| `init_schema()` | 无 → 无 | 建表（users / usage_records） |
| `load_from_csv()` | 无 → 无 | 从 CSV 导入，月份标签对齐到最近 N 个月（幂等，可重复跑） |
| `get_current_user_id()` | → str | 当前用户 ID（无用户系统，取配置里的演示账号） |
| `get_user_city(user_id)` | str → str | 查用户所在城市；不存在返回空串 |
| `get_usage_record(user_id, month)` | str, str → dict \| None | 查使用记录；未找到返回 None |

**初始化入口：** `uv run python -m agent.tools.external_data`（建表 + 导入 CSV）。
**连接策略：** 每次查询开短连接、用完即关（见 `_session()`）——SQLite 是嵌入式数据库，避开并发问题。

### agent 层

| 方法 | 输入 → 输出 | 说明 |
|---|---|---|
| `ReactAgent.execute_stream(会话消息列表)` | list[dict] → 生成器 | 驱动 Agent 流式运行，逐段 yield 文本 |
| 7 个工具 `.invoke({...})` | 参数字典 → str | 工具单测入口 |
| `trim_history.before_model(state, None)` | 状态 → dict | trim_history 的单元测试入口（中间件对象的方法） |
| `trim_history / log_before_model / monitor_tool / report_prom_switch` | 钩子 | 四扇门（中间件） |

---

## 10. 排查手册（常见问题定位）

| 现象 | 检查点 |
|---|---|
| 检索不到内容 | 1) chroma_db 是否在**项目根**（persist_directory 必须过 get_abs_path）2) md5.txt 是否记录了文件 3) 日志里"命中几条" |
| API 报 401 | .env 的 key 是否与平台一致（整行复制，勿手打）；load_dotenv 是否锚定项目根 |
| 模型不调工具 | 看日志：log_before_model 的"带有 N 条消息"是否增长；main_prompt 的工具手册是否被改坏 |
| 报告模式没切换 | 日志搜 `fill_context_for_report`（该调没调）→ `信号弹检测`（翻页没翻）→ `report_prom_switch`（换装没换） |
| 页面显示文档/代码 | st.xxx 是否漏了括号（忘括号 = 显示函数文档） |
| KeyError: session_state | 键名拼写不一致（messages vs message），Ctrl+F 全文统一 |
| 重复入库烧钱 | md5.txt 丢失或路径错 → 账本查不到记录 |
| 报告流程拿不到数据 | 1) `data/external/demo.db` 是否存在 2) 是否跑过 `uv run python -m agent.tools.external_data` 3) 当前月份的记录是否存在（导入时月份标签会对齐到最近 12 个月，覆盖不到就查不到） |
| 天气返回空字符串 | Open-Meteo 是**两步调用**（地理编码 → 取天气）。查日志 `[get_weather]`：城市名找不到、或网络不可达。工具按约定返回空串而非抛异常，模型会据此说明无法获取 |
| 控制台被大量调试信息刷屏 | 检查是不是有**库代码**在用 `print()`。库函数的调试输出一律用 `logger.debug`——按 `logger_handler.py` 的配置（控制台 INFO / 文件 DEBUG），它只进日志文件、不打控制台，既不刷屏也不丢排查细节。（`__main__` 测试块里的 `print` 是对的，别改） |
| 长对话突然"失忆"（忘了前几轮） | 消息数超过 `agent.yml` 的 `max_history_messages`，trim_history 裁掉了早期对话。日志搜 `[trim_history]` 确认；需要更长的记忆就调大上限 |
| 模型调用报 400 / 提示 tool_call 对不上 | 裁剪边界处理失效，留下了"孤儿 ToolMessage"。检查 trim_history 里那个跳过开头 `ToolMessage` 的 while 循环 |
| `uv run` 报 `trampoline failed to canonicalize script path` | 项目目录被**移动或重命名**过 → `.venv/Scripts/` 里的命令行工具启动器内嵌的是旧绝对路径。重建即可：`rm -rf .venv && uv sync`（`.venv` 可完全从 uv.lock 重建，项目数据都在项目根，不受影响） |

**日志位置：** `logs/agent_当天日期`；关键前缀：`[Tool monitor]`、`[trim_history]`、`[log_before_model]`、`[report_prom_switch]`、`[加载知识库]`。

---

*文档版本：2026-09-21*
